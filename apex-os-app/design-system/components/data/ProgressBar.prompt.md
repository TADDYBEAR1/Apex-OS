Hairline campaign progress track with a liquid cyan→violet fill and a glowing leading dot.

```jsx
<ProgressBar value={62} />
<ProgressBar value={40} height={2} showDot={false} />
```

`value` is 0–100. Drop `showDot` for a plain hairline (e.g. inside dense rows).
