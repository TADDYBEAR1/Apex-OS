// Kept in a tiny standalone module so App.jsx can route by slug
// without eagerly importing the heavy VisualShowcase bundle.
export const VISUAL_SHOWCASE_SLUGS = ['neon-forge', 'frosted-atlas', 'solar-carbon', 'prism-reactor'];
